# A tuple in python is a collection of ordered elements similar to lists however tuples cannot be changed or removed

my_tuples = (1, 2, 3, 4, 5)

print(my_tuples)
print(my_tuples[0])
print(my_tuples[2])
print(my_tuples[-1])

tuple1 = (1, 2, 3)
tuple2 = (4, 5, 6)

conc_tuple = tuple1 + tuple2
rep_tuple = tuple1 * 3

#print the tuples if you want i did 
# tuples has the same index laws as lists but we cannot change or replace

  